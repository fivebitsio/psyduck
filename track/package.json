{
  "name": "@psyduck/track",
  "type": "module",
  "version": "1.0.0",
  "private": true,
  "main": "track.js",
  "scripts": {
    "package": "bun run prebuild && bun build track.js --outdir dist --minify",
    "clean": "rm -rf dist",
    "prebuild": "bun run clean",
    "build": "bun run package",
    "dev": "bun run --watch track.js"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
